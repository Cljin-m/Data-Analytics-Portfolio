# Beauty E-commerce User Behavior Analysis with MySQL and Tableau

## 项目概览

本项目基于美妆电商用户行为数据，分析用户活跃、购买转化、留存复购、用户价值和商品表现。项目使用 MySQL 完成字段标准化、清洗和指标表构建，并使用 Tableau 搭建 Dashboard 展示流量、行为、留存、RFM、地域和商品维度分析结果。

## 业务 / 分析问题

- PV、UV 和浏览深度在日期和小时维度上如何变化？
- 用户浏览和购买行为主要集中在哪些时间窗口？
- 从浏览到购买的路径中，用户在哪些环节出现明显流失？
- 次日留存和 5 日留存表现如何？
- 如何通过 RFM 思路划分用户价值类型？
- 哪些商品品类具备高流量、高销量或高转化潜力？

## 数据集 / 数据来源

Markdown 报告标注的数据来源为阿里天池公开数据集：

```text
https://tianchi.aliyun.com/dataset/196209
```

报告中记录的字段包括：

- `user_id`：用户 ID
- `item_id`：商品 ID
- `behavior_type`：用户行为标签，1 = 浏览，2 = 收藏，3 = 加购物车，4 = 购买
- `item_category`：商品类别 ID
- `date`：日期
- `hour`：小时
- `user_geohash`：用户所在省份 / 地理字段

当前工作区未包含原始数据文件。

## 方法

- 使用 SQL 修改列名并标准化字段。
- 检查空值。
- 使用 `SELECT DISTINCT` 临时表识别并去除重复记录。
- 按日期和小时计算 PV、UV 和 PV/UV 浏览深度。
- 聚合浏览、收藏、加购、购买四类行为的时间序列。
- 聚合购买用户地域分布。
- 按用户购买次数计算复购率。
- 创建用户 - 地区 - 商品维度的行为序列 View，构建浏览、收藏 / 加购、购买路径。
- 构建三层转化漏斗：浏览、浏览-收藏/加购、浏览-收藏/加购-购买。
- 通过用户日期自连接计算次日留存和 5 日留存。
- 使用 Recency 和 Frequency 评分构建 RFM 用户分类。
- 统计商品品类人气、热销品类和各行为次数。
- 基于 SQL 输出结果在 Tableau 中搭建分析看板。

## 技术栈

- MySQL
- SQL
- Tableau
- Markdown

## 关键发现

- 报告指出 12 月 12 日是观察期内 PV 和 PV/UV 的峰值，UV 相比 PV 波动较小。
- 报告将 18:00-23:00 识别为核心流量与成交时间窗口，不同指标视图中高峰集中在 21:00-22:00 或 22:00 附近。
- 次日留存率整体高于 5 日留存率，说明短期回访强于较长期留存。
- 浏览到购买路径存在明显用户流失，说明商品曝光到最终购买之间仍有转化提升空间。
- 商品分析将品类区分为高浏览高购买、低曝光高购买、低曝光低购买和高浏览低购买等类型，用于支持差异化运营判断。

## 项目结构

```text
ecommerce-sql-tableau-analysis/
└── README.md
```

当前工作区中的原始报告和截图：

```text
../../电商美妆/运用MySQL和Tableau的电商用户分析.md
../../电商美妆/图片和附件/
```

报告入口：[运用MySQL和Tableau的电商用户分析.md](../../电商美妆/运用MySQL和Tableau的电商用户分析.md)

## 如何运行

当前工作区包含的是写在 Markdown 报告中的 SQL 逻辑，不包含独立 SQL 脚本或 Tableau Workbook。复现流程如下：

1. 将报告中引用的天池数据集导入 MySQL，并命名为 `customers_beauty_data`。
2. 按报告顺序执行 SQL 代码块。
3. 使用生成的 SQL 表和 View 作为 Tableau 数据源。
4. 根据原始报告中的截图查看或复现 Dashboard。

## 输出

- SQL 清洗语句和分析指标表。
- 日期 / 小时维度 PV、UV 和浏览深度指标。
- 用户行为时间序列表。
- 购买地域分布表。
- 复购率查询结果。
- 行为序列 View 和转化漏斗表。
- 留存率表。
- RFM 分类表。
- 商品品类热度和行为统计表。
- Tableau Dashboard 截图。

